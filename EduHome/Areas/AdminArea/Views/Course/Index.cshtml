@model IEnumerable<Course>
<div class="container">
    <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Course Table</h4>
                    <a class="btn btn-outline-github" asp-action="create">
                        Create
                    </a>
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>Image</th>
                                    <th>Name</th>
                                    <th>Description</th>
                                    <th>Settings</th>
                                </tr>
                            </thead>
                            <tbody>

                                @foreach (var course in Model)
                                {
                                    <tr>
                                        <td>
                                            <img src="~/img/course/@course.ImgUrl" class="rounded-0" style="width:120px ; height:120px" alt="" />
                                        </td>
                                        <td>@course.Name</td>
                                        <td>@course.Desc</td>
                                        <td>
                                            <a class="btn btn-outline-facebook" asp-action="detail" asp-route-id="@course.Id"><i class="mdi mdi-account-card-details"></i></a>
                                            <a class="btn btn-outline-primary" asp-action="update" asp-route-id="@course.Id"><i class="mdi mdi-settings"></i></a>
                                            <a class="btn btn-outline-danger" asp-action="delete" asp-route-id="@course.Id"><i class="mdi mdi-delete"></i></a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>